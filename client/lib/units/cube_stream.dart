class CubeStream{
  final String fid; //User id
  final String id; //Unique id of cube
  final String url;
  final String type;
  final String source;
  final String opType;

  CubeStream(this.fid, this.id, this.url, this.type, this.source, this.opType);
}